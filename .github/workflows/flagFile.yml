# This is a basic workflow to help you get started with Actions

name: FlagFile

on:
  push:
    branches: [ "main" ]


  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: Generate text file
        run: |
          echo 'Test Manual' > testFile.txt
      - name: Move to output
        run: |
          mkdir -p output yes| cp -rf testFile.txt ./output/
      - name: Commit files # commit the output folder
        run: |
          git config --local user.email "justin.pfefferle@codence.com"
          git config --local user.name "GitHub Action test"
          git add ./output
          git commit -m "Add changes"
      - name: Push changes # push the output folder to your repo
        uses: ad-m/github-push-action@master
        with:
#           branch: B77 #ignore if your branch is master
          github_token: ${{ secrets.GITHUB_TOKEN }}
          force: true
      - name: Set current date as env variable
        run: echo "NOW=$(date +'%Y-%m-%dT%H:%M:%S')" >> $GITHUB_ENV
#       - uses: "finnp/create-file-action@master"
#         env:
#           FILE_NAME: "flagFile.txt"
#           FILE_DATA: "Test"
